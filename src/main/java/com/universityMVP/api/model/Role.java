package com.universityMVP.api.model;

// Enum to represent user roles in the system
// Not is a table in this case, is just restriction name to the user role
public enum Role 
{
    STUDENT,
    TEACHER,
    ADMIN
}